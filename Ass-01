#include<stdio.h>
int main()
{
	char ch;
	scanf("%c",&ch); 
	if((ch >= 'a' && ch <= 'z') || (ch >= 'A' && ch <= 'Z'))
	printf("It is a alphabet");
	else
	if(ch >= '0' && ch <= '9')
	printf("It is a digit");
	else
	printf("Spl symbol");
}
